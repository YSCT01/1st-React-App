{
  "name": "gif",
  "private": true,
  "version": "0.0.0",
  "homepage": ".",
  "scripts": {
    "dev": "vite",
    "start": "vite",
    "build": "vite build && rm -rf docs && mv build docs",
    "preview": "vite preview",
    "predeploy":"npm run build",
    "deploy":"gh-pages -d build && rm -rf docs && mv build docs"
  },
  "dependencies": {
    "prop-types": "^15.8.1",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "@vitejs/plugin-react": "^1.3.0",
    "gh-pages": "^4.0.0",
    "vite": "^2.9.7"
  }
}
